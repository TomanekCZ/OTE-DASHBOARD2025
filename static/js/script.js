// Import shared utility functions
// Include utils.js before this script in HTML

// Initialize date/time display and update it every second
setInterval(updateDateTime, 1000);

// Call updateDateTime immediately to avoid initial delay
updateDateTime();

// Removed the fetchData function and chart creation functions

// Removed the main
